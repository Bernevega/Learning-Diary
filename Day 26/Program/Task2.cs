using System;

class Lamp
{
    /*
    -isOn: bool
    -brightness: int
    +Brand: string
    +TurnOn(): void
    +TurnOff(): void
    +ChangeBrightness(amount: int): void
    */
}

class Task2
{
    public static void TaskTwo()
    {
        /*
        what data the lamp stores:
            if it's on, its brightness and brand
        what actions the lamp can do
            switch on/off and change brightness
        which members are probably fields
            isOn and brightness
        which member is a property
            Brand
        */
    }
}